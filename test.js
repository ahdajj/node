/*
setTimeout(()=> {        // this will run once after 3 sec
    console.log('in the timeout')
    clearInterval(int)
}, 3000);

const int=setInterval(()=>{         // this will run every 1 sec
console.log('in the interval')
}, 1000) 
*/

console.log(__dirname);
console.log(__filename);

